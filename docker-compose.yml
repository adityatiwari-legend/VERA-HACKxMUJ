services:
  vera-db:
    image: postgres:16-alpine
    container_name: vera-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: vera
    ports:
      # Mapped to host port 5433 for local host development/testing
      - "5433:5432"
    volumes:
      - vera_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d vera"]
      interval: 5s
      timeout: 5s
      retries: 5

  vera-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vera-app
    restart: unless-stopped
    depends_on:
      vera-db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://postgres:postgrespassword@vera-db:5432/vera
      JWT_SECRET: vera-super-secret-jwt-key-for-development-32chars!
      NEXT_PUBLIC_APP_URL: http://localhost:3000
      PORT: 3000
      STORAGE_PATH: /app/storage
      MAX_UPLOAD_SIZE_MB: 50
    ports:
      - "3000:3000"
    volumes:
      - vera_storage:/app/storage

  nginx:
    image: nginx:alpine
    container_name: vera-nginx
    restart: unless-stopped
    depends_on:
      - vera-app
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - certbot_etc:/etc/letsencrypt
      - certbot_var:/var/www/certbot

volumes:
  vera_postgres_data:
    driver: local
  vera_storage:
    driver: local
  certbot_etc:
    driver: local
  certbot_var:
    driver: local
